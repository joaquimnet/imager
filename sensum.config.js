/**
 * @type {import('sensum').IConfig}
 */
const config = {
  token: process.env.DISCORD_TOKEN,
  ownerId: '517599684961894400',
  prefix: 'i.',
  debug: true,
  name: 'Imager',
  token: process.env.DISCORD_TOKEN,
};
module.exports = config;
